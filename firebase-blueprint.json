{
  "entities": {
    "Winner": {
      "title": "Winner",
      "description": "A weekly winner entry",
      "type": "object",
      "properties": {
        "firstName": {
          "type": "string",
          "description": "First name of the winner"
        },
        "middleName": {
          "type": "string",
          "description": "Middle name of the winner"
        },
        "lastName": {
          "type": "string",
          "description": "Last name of the winner"
        },
        "name": {
          "type": "string",
          "description": "Full name of the winner (legacy)"
        },
        "day": {
          "type": "integer",
          "description": "Day of the month (1-31)"
        },
        "month": {
          "type": "string",
          "description": "Month of the year"
        },
        "year": {
          "type": "integer",
          "description": "Year of the win"
        },
        "isNoWinner": {
          "type": "boolean",
          "description": "True if no winner was selected for this period"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the entry was created"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the entry was last updated"
        }
      },
      "required": ["firstName", "lastName", "day", "month", "year"]
    }
  },
  "firestore": {
    "/winners": {
      "schema": "Winner",
      "description": "Collection of weekly winners"
    }
  }
}
